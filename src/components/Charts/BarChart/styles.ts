import styled from 'styled-components';
import { VictoryContainer } from 'victory';

export const Container = styled.div``;

export const Content = styled.div``;

export const VictoryChartStyled = styled(VictoryContainer)`
  height: 37%;
  width: 37%;
  user-select: none;
  pointer-events: none;
  touch-action: none;
  position: relative;
`;
