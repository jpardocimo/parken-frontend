/* eslint-disable no-shadow */

// When updating this enum, you must also update the respective Enum in the backend project.
enum EnumProductUserGroup {
  Internal = 'Internal',
  External = 'External',
}

export default EnumProductUserGroup;
