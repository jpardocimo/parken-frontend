/* eslint-disable no-shadow */

// When updating this enum, you must also update the respective Enum in the backend project.
enum EnumCategoryType {
  Apillar = 'A-Pillar',
  Topic = 'Topic',
  Subtopic = 'Subtopic',
  Question = 'Question',
}

export default EnumCategoryType;
